import 'package:flutter/material.dart';

class DisplayMoney extends StatelessWidget {
  const DisplayMoney({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: const Color(0xFF2DBD3A));
  }
}